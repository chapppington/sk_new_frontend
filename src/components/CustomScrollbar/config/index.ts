export const SCROLLBAR_CONFIG = {
  desktopBreakpoint: 768, // Minimum width for desktop devices
  thumbHeight: 100, // Fixed thumb height in pixels
  navbarHeight: 72, // Fixed navbar height in pixels
} as const
